services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: chef_table
    ports:
      - "5432:5432"
    volumes:
      - chef_table_db:/var/lib/postgresql/data
    profiles: ["db", "app"]

volumes:
  chef_table_db:
